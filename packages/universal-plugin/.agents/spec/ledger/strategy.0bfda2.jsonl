{"seq": 1, "handle": "sdd-scanner", "kind": "strategy", "distills": "github-89-build-axi", "recommendation": "This deliver-only mission (implement plugin build's AXI output surface against an already-frozen build.feature) carried one judge-iteration correction worth codifying beyond its own fix: the cold impl-judge caught that the e2e test's OWN ASSERTION had drifted from the frozen oracle, not just the implementation — build.feature requires a top-level 'built' array in --format json output, the first implementation cut emitted written/rows/summary with no 'built' array, AND the e2e assertion had independently drifted to check 'written' instead, so a naive read of \"tests are green\" would not have caught the contract miss until the cold judge re-derived the oracle from the frozen scenario text rather than trusting the existing assertion. This reinforces (does not duplicate — different mechanism) the doctrine's existing 'a source-read is a hypothesis' principle: an existing PASSING test is itself a hypothesis about conformance to the frozen contract, not proof of it, when the assertion could have drifted independently of the scenario it's meant to check. No new ledger action needed beyond noting it as corroborating evidence for that already-established principle. Separately, this correction's `cause` field (\"format-json-missing-built-array\") is off-enum against combat-log-governance's cause list — see the cross-referenced cause-enum-conformance entry in the sdd project ledger, which uses this instance as one of its two pieces of evidence.", "evidence": [".agents/plans/github-89-build-axi.log.jsonl: \"Cold impl-judge rejected --format json: frozen build.feature requires stdout JSON with a 'built' array; first cut emitted written/rows/summary with no 'built' array and the e2e assertion had drifted to check 'written'... Re-judged: all 18 frozen scenarios pass.\"", "packages/universal-plugin/.agents/spec/ledger/github-89-build-axi.acd957.jsonl: impl gate approve, \"One judge-iteration correction (--format json missing the frozen 'built' array) recorded and fixed before this gate\"; project spec advanced approved -> implemented; PR closed #89 (commits 79620344 / 64e70f86, 2026-07-12)", "cross-ref: .agents/specs/sdd/ledger/strategy.0bfda2.jsonl seq2 (cause-enum-conformance early signal, sdd project ledger, this same Scanner session/shard hash)"], "ratified": false}
