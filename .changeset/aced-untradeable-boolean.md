---
"@cyberplace/aced-plugin": patch
---

ACED selection gains a second, disjoint ground: a graded criterion spanning the whole of what a subject produces is untradeable by construction and is authored as one boolean `@quality` scenario, never a `@rubric` dimension — no boolean twin required (CR-7).
