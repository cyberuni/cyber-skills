{"kind":"leash","cr":"quill-docs-section","leash":"auto-spec","by":"derived","blast":"medium — six new behavioral leaves under a new content/docs/quill/ grouping in a status: draft project with no frozen suite outside instruction-target; two of the six documents already exist and one of those is factually stale against the live registry. No existing frozen scenario is touched, so no Clearance floor fires; the amendment to the root spec's backfill-gap counts is a documented-layout edit, not a narrowing.","approach":["target the website project spec, not quill's — the artifact is a website page; quill is the subject","authority for role/bar bindings is .agents/universal-plugin.json, never the existing prose","dogfood for real: quill-spec-writer authors, quill-doc-writer writes, quill-judge runs the checks","a defect found in quill itself is a followup, never fixed inside this CR"]}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the plugin readme omits the governances block entirely","note":"the readme's role table lists the five production-chain roles and never mentions the two bar governances the registry binds, so a readme reader cannot learn that quill-builder-spec and quill-builder-impl are bound at all. This is the omission the website page turned into a positive false claim."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the plugin readme still ships the retracted recurrence rule","note":"the readme's integrity row leads with a claim landed twice being a defect, which the doc-eval model retracts on empirical grounds; the readme also classes all listed inter-passage defects as inspection, where the model's own correction makes three of them judged. The readme predates the judged instrument entirely."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"quill and the SDD plugin contract disagree on what a null spec-judge degenerates to","note":"three quill artifacts state the spec gate applies the documentation criteria itself with no judge agent; the SDD plugin contract and the spec gate both resolve an unfilled slot to the SDD default and spawn the cold default judge unconditionally. One of the two is wrong and the website contract was written to survive either resolution."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the registration skill's reject-a-missing-governances-block rule has no reachable trigger","note":"the skill constructs the only payload it ever writes and writes the block itself, so the stated rejection cannot fire from inside its own workflow. Either it should validate an existing entry it is about to rewrite, or the rule belongs to the registry reader."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the glossary hard-codes the catalog entry count","note":"the defect-catalog term is defined with a fixed count of entries, coupling the ubiquitous language to a number the catalog will move."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the doc-eval model's instrument table undercounts the judged tier","note":"the table gives the judged instrument's scope as one check per document, while the bar it points to carries one inspection rule plus the whole catalog per document. The cell reads as though the document-scoped pass were a single check."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the calibration table has no state for an entry that ran and never fired","note":"the table admits advisory and calibrated only, while the prose requires that an entry which never fires on either document has not been calibrated but merely untested. That outcome is currently indistinguishable from never having been run."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the spec bar states no converse of its use-case tracing rule","note":"the bar requires every entry point to trace to a coverage row, but never flags a coverage row no entry point reaches. It carries the symmetric rule for audiences and for coverage-versus-scenarios, so the missing direction reads as an oversight."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the spec bar leaves the doc-type-to-north-star join implicit","note":"each doc type declares what success is, but the north-star element is type-agnostic, so a spec can declare one type and write another type's north star with nothing catching it. A retrieval claim and a capability claim grade differently."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"apps/website/.agents/spec/","title":"page nodes and quill's own nodes disagree on the concept tag's shape","note":"this project's page nodes carry a list where quill's registry node carries a scalar. Harmless per node, but it is the kind of drift the concept index is derived from."}
{"kind":"followup","cr":"quill-docs-section","class":"blocking","target":".agents/specs/quill/","title":"all three quill agents replace the SDD producer load list instead of extending it","note":"contradicts this mission's claim to have run quill's production chain end to end at the SDD gates. The SDD spec-producer procedure requires loading the resolved oracle, builder and architect actor bars forward, so the producer self-aligns to the lens set it will be graded against; quill's spec-producer definition names only its own builder bar plus the two format bars and the ownership matrix, so a producer following its own definition faithfully cannot clear the spec-judge pre-flight. The same omission is in the doc-writer and the judge, which name only the impl builder bar and the ownership matrix. The sibling plugin's agents name the full set, so the correct shape already exists in the corpus and this is a quill defect, not a gate defect. Six of six cold spec-judges blocked on it independently."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"plugins/sdd/","title":"the spec-judge pre-flight expects two governances the producer procedure never requires","note":"the judge's expected set includes the lifecycle and gate-validation bars, while the SDD spec-producer procedure's own required-load list names neither. A producer following that procedure to the letter therefore cannot clear the check on those two names. Either the producer procedure should require them or the pre-flight should not expect them; as it stands the check is unpassable by an otherwise conforming producer."}
{"kind":"followup","cr":"quill-docs-section","class":"blocking","target":".agents/specs/quill/","title":"evidence that the omitted forward bars change spec content, not just provenance","note":"appended to the agent-definition followup. All six nodes were re-authored with the three previously-omitted forward bars loaded. Six of six carried a real defect only those bars caught, and none were cosmetic. Six of six had a double-barreled north star failing the Oracle bar's name-the-outcome-without-and test. The spec-bar node documented only the plugin half of a union bar, so a reader following that page would have written a conforming suite and still failed the gate. Two nodes had a control-flow edge no scenario covered, one had a decision node with a dead branch, one had a lone negative with no positive companion, and one had link scenarios asserting a sibling page's prose rather than its ownership, which would have broken when that sibling was trimmed in the same change request. The four-governance definition is therefore not a paperwork gap; it degrades every documentation spec quill authors."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"plugins/sdd/","title":"a recorded-conflict note has no named home in the spec format","note":"a node that records two source authorities contradicting each other, and writes its contract to survive either, is not recording a design fork, so the format's routing of chosen-versus-rejected narrative to the unit solution file does not fit it. Two nodes in this corpus now park such a note as a subsection of References, held together by convention rather than by the format. If the shape recurs it wants a named section in the spec format governance. Raised by a producer defending its own placement, and the precedent it cited was verified."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the spec bar's completeness element never requires spending each coverage row by ID","note":"measured. The bar requires a completeness check but not that it account for each row individually, so a summary claim can quantify over members it does not cover. Three instances across two nodes and the dominant regression class of the mission: a range that hid a row, an intro asserting every entry point ends at a destination when two did not, and a completeness clause claiming a closure a narrowed scenario no longer provided. One producer independently invented the missing rule and wrote it into its own spec."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"no rule that a node's identifiers share one namespace","note":"measured. Two nodes independently shipped an identifier collision: a control-flow start node colliding with a use-case group in the same scenario-map tables, and one token used for both a coverage row and a catalog entry. One was introduced by a remediation. Enumerated items should be referenced by name, never by an invented index."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"the doc-type table is not joined to the north-star element","note":"each declared doc type states what success is, but the north-star element is type-agnostic, so a retrieval claim and a capability claim are graded the same. Producers had to make the join themselves on every node that declared reference."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":".agents/specs/quill/","title":"no intra-node reconciliation duty after an edit","note":"measured. Four of four remediated nodes regressed in one round, every finding introduced by the remediation rather than surviving it. Three were the same shape: the claim was changed where the finding pointed and the passages referencing it were left stale, including a feature-file preamble three lines above the scenarios that contradicted it. A sweep is not self-certifying either: the node piloted through one caught five of six targeted items plus four unprompted defects and still left one claim false."}
{"kind":"gate","cr":"quill-docs-section","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","why":{"floor":"none — six new behavioral leaves under a new grouping, all additive. No pre-existing frozen scenario anywhere in this project was narrowed, rewritten, or re-opened; before this CR the project carried no frozen suite at all. The root spec edits are a documented-layout reconciliation, not a narrowing.","blast":"medium — six nodes and a new descriptive grouping in one section, plus the root spec's node table, backfill-gap counts and by-concept index. Sibling sections untouched. Two of the six documents already existed and both were factually stale against the live registry.","novelty":"medium — the first website section specified whole rather than page by page, which is what forced an explicit one-claim-one-owner boundary contract; and the first end-to-end dogfood of quill's own production chain.","confidence":"high — six of six cold sdd-spec-judges return {oracle,builder,architect} all PASS with ALIGNED true, each having independently re-derived counts, set equality and namespace disjointness rather than accepting the producers' reports. check-suite and check-spec-state clean on every node; root check:specs green including the plan-safety guard. Five gate rounds: findings 8 then 5 then 3 then 0, with round 3's all-self-inflicted inversion stopped for a re-plan rather than another round. Self-asserted within an auto-spec leash — ratify or kick back.","cr":"quill-docs-section"}}
{"kind":"gate","cr":"quill-docs-section","gate":"impl","verdict":"approve","by":"agent","cause":"dimension","why":{"floor":"none — no frozen scenario was narrowed at impl. The one impl-gate failure was fixed in the document, never in the suite: a scenario required a link to the governance owning the union bar's other half and the page named it without one.","blast":"medium — six documents published (four new, two revised, both of which were factually stale against the registry), plus four sidebar entries. No source outside the website app changed.","novelty":"medium-high — first end-to-end dogfood of quill's own chain: quill-spec-writer authored, the SDD default judge graded, quill-doc-writer wrote and co-produced the acceptance checks, quill-judge ran them. The judged tier ran blind-then-scored for the first time on real documents.","confidence":"high — 125 of 125 frozen scenarios pass across six suites, each verified by a cold quill-judge running the producer's recorded checks plus a document-scoped enumeration pass and a blind reader simulation scored against the defect catalog. Judges verified claims rather than accepting them: one re-derived the registry bindings directly, one traced the forwarding anchor to the line that defines it, one caught a producer's verification check that had softened a frozen Then from links to names. All judged findings advisory, none blocking, as the calibration state requires. Website build green, 81 pages. Self-asserted within an auto-spec leash — ratify or kick back.","cr":"quill-docs-section"}}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"apps/website/.agents/spec/","title":"the missing-element rule routes four of the seven elements it enumerates","note":"found at the impl gate on the spec-bar page and confirmed by two judges independently. The page enumerates seven required elements and routes only four to the content-gap remedy; why the document exists, non-goals and prerequisites are unrouted. The frozen scenario ratifies the four-member list, so the scenario outranks the document-scoped rule and the page is correct against its suite. The gap is in the spec: either extend the assertion to all seven or state why the other three route through the present-but-not-passing rule instead."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"apps/website/.agents/spec/","title":"three prerequisite glosses missing on the eval-model page, each tripping a blind reader","note":"the node's prerequisites row names three terms needing a gloss and omits bar, producer and catalog. All three are load-bearing on a declared route and all three tripped the blind reader simulation. Repairable without breaching the boundary contract, since glossing a term is not enumerating a catalog."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"apps/website/.agents/spec/","title":"the entry page cannot tell a reader which of the five artifact types to declare","note":"the coverage row requires the five types be named as keys but nothing requires distinguishing them, and no control-flow leaf owns the question. A blind reader holding a concept document ended on a coin flip. Related: the intersection of declared-as-one-of-the-five and not-structurally-checkable has no route, because the two gates are drawn sequentially so that case never arises."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"apps/website/.agents/spec/","title":"one registration scenario is unsatisfiable on a strict reading against its own suite preamble","note":"the preamble bars a link set quantifying over the bars quill authors while the scenario asks for a link to the page owning what a bar requires. The two-hop redirect through the bindings page is the only reading satisfying both, and the impl-judge accepted it, but a cold judge without the spec in hand could plausibly fail it. Reconcile in the spec."}
{"kind":"followup","cr":"quill-docs-section","class":"backlog","target":"apps/website/","title":"no SDD actor bar has a page, so any plugin bar unioning onto one has nowhere to link","note":"the SDD section publishes the workflow but none of the five actor-bar governances, while the quill section publishes both of its bars in full. A page documenting a union bar must therefore route through the workflow overview's table rather than to a peer page. This CR hit it once and worked around it honestly; a page per SDD actor bar would close the class."}
