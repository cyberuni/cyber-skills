{"kind":"leash","cr":"github-485","leash":"auto-all","by":"derived","blast":"low — one published article and its own node contract (README + suite). The node's .feature is unfrozen (backfilled for calibration, never gated), so no Clearance floor can fire; the revision is additive coverage plus a clarifying qualification on an existing rung, and the underlying model in artifacts/specs/motive-model/spec.md already carries both missing pieces, so no theory changes.","approach":["treat this as a transcription gap on the page, not a model change — the project spec already states loop placement and that agent configuration is itself a product","keep the ladder; qualify it. The defect is reading a characteristic scope as a sufficient test, not the rungs being wrong","forward to scenarios.mdx for the three loops rather than duplicating its contract — it is downstream in the reading order","every new scenario must fail against the pre-fix prose"]}
