{"seq": 1, "handle": "sdd-scanner", "kind": "strategy", "distills": "376-doctrine-validate-before-draft", "recommendation": "376 shipped the validate-before-draft gate this very Scanner pass now runs under (dogfood: it fired 9 minutes after PR #379 merged, but that earlier pass was aced-scoped and missed this sdd-project CR entirely — a corpus-wide sweep catches it). Two real corrections from its own combat log are worth codifying. (1) A standard coverage-gap: the spec judge caught an orphaned agent-filed-marker claim and a missing log-side companion for the new disposition field, fixed by adding the disposition:open|resolved pairing to both sides. (2) More novel: the architect rejected a resolved-disposition pending-count scenario staged inside scanner.feature as a co-owned seam property that belongs to the consumer node (gateway), which neither documented nor tested the exclusion — fixed by rewording scanner's scenario to a write-time entry property and adding the consumer-side scenario in gateway/. That correction's cause was written as \"node-boundary\", which is NOT in combat-log-governance's current cause enum (coverage-gap | design-overreach | spec-feature-contradiction | prose-impl-contradiction) — validated by reading the live governance skill body just now, confirmed absent. Recommend the Council decide whether \"node-boundary\" (a scenario asserting a co-owned/cross-node property that belongs in a sibling unit's suite) earns enum growth, or whether it should be folded into an existing value at the next real recurrence — see the sibling cause-enum-conformance entry in this same shard for the broader pattern this single instance feeds.", "evidence": [".agents/plans/376-doctrine-validate-before-draft.log.jsonl seq2: \"spec gate: architect rejected the resolved-disposition pending-count scenario as a co-owned seam property staged inside scanner.feature (a different unit's behavior)... Fixed by rewording scanner's scenario to a write-time entry property... and adding the consumer-side scenario + doc sweep to gateway/.\"", ".agents/specs/sdd/ledger/376.c67cab.jsonl seq2-3: both spec and impl gate self-asserted ALIGNED/PASS, root status implemented; PR #379 merged 2026-07-24T10:33:35-07:00", "sdd:combat-log-governance (read fresh this session): the cause enum table lists exactly four grounded values; \"node-boundary\" is absent from all four"], "ratified": false}
{"seq": 2, "handle": "sdd-scanner", "kind": "strategy", "recommendation": "Early signal, small n (2 of 3), flagged now rather than waited-out: the two correction lines this pass distilled from the current LIVE combat-log corpus (376's \"node-boundary\", github-89-build-axi's \"format-json-missing-built-array\") both write a `cause` value absent from combat-log-governance's enum — only 376's other correction (\"coverage-gap\") lands on-enum. github-89's value in particular reads as a bespoke per-defect label (naming the specific missing field) rather than a categorical bucket, which is exactly the shape the enum exists to prevent — combat-log-governance states an absent or off-enum cause \"fails closed (it breaks cross-mission matchability)\", so these two corrections are currently invisible to the doctrine loop's own recurring-pattern detector even though they are real, current, on-disk defects. Recommend a cheap durability nudge at the point the conductor writes a `correction` line: prefer an enum value; if none fits, write the off-enum string AND flag it explicitly (e.g. a `cause-candidate: true` marker or a note in `detail`) so an off-enum cause reads as \"proposed for enum growth\" rather than silently falling out of recurrence counting. Do not treat this as proof of a systemic problem yet — 2 instances is not a recurrence, and both may simply reflect that neither github-89-build-axi's nor 376's specific defect had a pre-existing enum home; re-evaluate if a third off-enum instance lands.", "evidence": ["live corpus histogram over every currently-tracked .agents/plans/*.log.jsonl correction line (grep \"cause\" across all 19 live combat logs): node-boundary x1, format-json-missing-built-array x1, coverage-gap x1 — exactly 3 correction lines exist corpus-wide right now, 2 off-enum", "sdd:combat-log-governance, correction cause table: \"An absent or off-enum cause fails closed (it breaks cross-mission matchability)\"", "cross-ref: strategy.0bfda2 seq1 (this shard) distills the node-boundary instance from 376; packages/universal-plugin/.agents/spec/ledger/strategy.0bfda2 seq1 distills the format-json-missing-built-array instance from github-89-build-axi"], "ratified": false}
