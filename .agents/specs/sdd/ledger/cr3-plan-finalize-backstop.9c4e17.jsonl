{"seq": 1, "kind": "leash", "cr": "local-cr3-plan-finalize-backstop", "leash": "auto-none", "by": "user", "blast": "medium", "approach": ["no-spike", "gated-cr-council-ratifies", "validate-premise-against-current-code", "corpus-sweep-of-all-plan-briefs", "surface-terminal-status-as-open-decision"]}
{"seq": 2, "kind": "followup", "cr": "local-cr3-plan-finalize-backstop", "class": "backlog", "summary": "propose a correction cause enum value for a producer that skipped its governance pre-flight — round 1 was filed under the nearest bucket coverage-gap, which blurs it with missing-scenario rejections in the recurrence detector"}
{"seq": 3, "kind": "followup", "cr": "local-cr3-plan-finalize-backstop", "class": "backlog", "summary": "mission/checkpoint already owns the todos-plus-NEXT write mechanism under its own trigger table; the finalize backstop stands as a second independent implementation. Triggers are genuinely distinct (pause vs this-session landing) so it is not a duplicate edge, but a structural pass should ask whether finalize should route through checkpoints existing write path"}
{"seq": 4, "kind": "gate", "cr": "local-cr3-plan-finalize-backstop", "gate": "spec", "verdict": "approve", "by": "unional", "cause": "dimension", "frozen": ["mission/conductor/conductor.feature"], "why": "Plan-finalize backstop: at finalize a landing mission reconciles its plan brief's todos and NEXT anchor to the landed state — the execution-state sibling of the already-frozen correction-line finalize backstop in the same node. 9 additive scenarios, 0 modified / 0 removed against the pre-CR base, so conductor.feature self-clears and stays @frozen; no Clearance owed. Placed on mission/conductor rather than mission/handoff: the sibling backstop already lives here, the plan brief is the conductor's own execution-state artifact, and handoff's Non-goals exclude writing frontmatter. GATED CR — leash auto-none, no self-assertion at any point; the Council ruled the open question and ratified this gate in-session, positionally. Ruling: Option B — the plan-level status stays the dispatch-flag-only enum active|approved, the backstop writes no terminal value, terminal-ness stays derived (todos-all-done AND source-closed), matching the same ruling made for the Scanner realization in scanner-stale-plan-status; a 9th guard scenario freezes it. Four cold spec-judge rounds, each self-observed, no verdict relayed: r1 blocked at the governance pre-flight (producer had loaded 1 of 7 required bars), r2 builder FAIL on a Then asserting a sibling node's outcome that this act records nowhere, r3 all three lenses pass with ALIGNED false on the deliberate open marker, r4 all three lenses pass and ALIGNED true with the marker resolved. Two backlog follow-ups recorded: a cause-enum value for a skipped producer pre-flight, and whether finalize should route through mission/checkpoint's existing todos+NEXT write path. Residual, pre-existing and node-wide: the conductor node carries no Control Flow or Scenario map section, so check-suite skips the binding check. Root pnpm verify 35/35; root status stays implemented (per-unit CR)."}
