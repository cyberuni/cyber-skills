{"kind": "leash", "cr": "aced-result-provenance", "leash": "auto-spec", "by": "derived", "approach": ["fix the producer, not the reader: run records what it consumed so freshness is a comparison, not an inference", "additive scenarios only on run's frozen suite; any narrowing stops the mission", "each defect the rejected prior attempt shipped becomes a binding scenario, so the design cannot regress into it", "the untrusted-pass problem stays out of scope and is filed as a follow-up, never folded in here", "self-assert the spec gate within leash; bring the impl gate to the owner"], "blast": "medium — changes the result record shape, a producer contract every downstream consumer reads, and adds one behavioral node beside run/compare/report. Bounded by additivity: legacy records carrying no evaluated set read absent, never current, so nothing pre-existing is silently reinterpreted.", "novelty": "medium — recording provenance is ordinary; the closed-world treatment (a consumed directory listing recorded as a hashed entry, so growth is caught without re-resolving the subject) is the one non-obvious move, and it is pinned by a positive scenario that fails an implementation which re-resolves instead.", "floor": "none — gherkin-cli structural diff against main reports addOnly true, 7 added / 0 modified / 0 removed on run.feature; both frozen persistence scenarios untouched. No Clearance, no Compatibility ceiling breach, no Conflict."}
{"kind": "gate", "cr": "aced-result-provenance", "gate": "spec", "verdict": "approve", "by": "unional", "cause": "clearance", "why": "run now records the inputs it reports consuming (path + SHA-256; a listed directory hashes its returned entry names AND yields a per-file entry), and the new check-freshness node compares those hashes against the tree for a current/stale/incomplete/absent verdict. Three cold judges over three rounds: final round all three lenses PASS on both nodes, ALIGNED true, CONFORMANCE ok, BLOCKER null, no open markers, every scenario binding with a named failing implementation. Round 3 killed a directory-only recorder that passed the suite while defeating check-freshness's stale verdict for content edits inside a recorded directory. Clearance granted live by the owner for one frozen scenario's wrong destination (no intended winner, no additive repair). Limits recorded not hidden: under-reporting is unbindable here, check-freshness ships unconsumed pending the run/improve wiring follow-up, and three pre-existing CFG edges are named as uncovered in place of the prior false completeness claim.", "frozen": ["eval-run/run/run.feature", "eval-run/check-freshness/check-freshness.feature"]}
{"kind": "gate", "cr": "aced-result-provenance", "gate": "impl", "verdict": "approve", "by": "unional", "cause": "dimension", "why": "Two cold impl-judges, one per node, both IMPLEMENTATION_PASS with no failing scenario and no contradiction against the frozen suites. run/ 8/8 in scope, purely additive (+45/-0), no pre-existing scenario regressed. check-freshness 22/22, every scenario separately confirmed to be BOUND by a discriminating fixture rather than merely passing \u2014 the judge mutated the stale/incomplete ordering, folded mtime into the file hash, and deleted the listing sort, and in each case exactly the targeted scenario failed and nothing else. Three real defects surfaced and were fixed at this gate: the directory-listing sort was unfalsifiable on ext4 (a mutant deleting it kept 26/26 green while breaking APFS/NTFS as a permanent wrong `stale`), now bound by a filesystem-independent unit test; SKILL.md's claim that the results directory is git-ignored was false, which this CR made expensive by writing a hash manifest per run; and the <aced-skills> gloss resolved to a path that does not exist. A conductor-introduced regression was also caught \u2014 the mtime paragraph split the entry-kind table between its rows, leaving the canonical-form contract rendering as a stray paragraph. Limits carried forward unchanged and recorded: under-reporting is unbindable by any scenario here (#475), and check-freshness ships consulted by nobody until the run/improve wiring lands (#476).", "frozen": []}
