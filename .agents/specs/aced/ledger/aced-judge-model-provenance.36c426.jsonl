{"kind": "leash", "cr": "aced-judge-model-provenance", "leash": "auto-all", "by": "derived", "approach": ["additive only across three sibling nodes in one capability; no frozen scenario narrowed, no new hard floor, owner pre-scoped the change and capped judge rounds at 3"]}
{"kind": "followup", "cr": "aced-judge-model-provenance", "class": "backlog", "what": "model-aware selection of a prior result: the readers of persisted records (report's trend, improve's reuse of the latest result) should prefer a prior scored under the same model, and say so when none exists rather than comparing across models silently", "why": "this CR records the scoring model and deliberately wires no consumer; the selection rule is the capability the field exists to enable", "note": "owner deferred out of this phase"}
{"kind": "followup", "cr": "aced-judge-model-provenance", "class": "backlog", "what": "user-declared model series in .agents/aced/models.toml, so a reader may fall back to a prior from the same series when no same-model prior exists", "why": "the fallback needs a grouping and an ordering that ACED cannot infer from a model id without inventing a provider-shaped taxonomy that rots on a rename", "note": "owner chose user-declared over inferred; blocked on the selection follow-up above"}
{"kind": "gate", "cr": "aced-judge-model-provenance", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "three cold spec-judge rounds all returned ALIGNED false; the owner then cut cross-model comparison from the phase, reverting the report band to base, and every unresolved finding lived only in that band (the comparability rule's domain gating, the unknown-vs-unknown crosswalk). What remains is the recording band, which round 3 passed on the builder lens with a named plausible wrong config per scenario - including the cheap copier that lifts eval.md's declared model into the record. The two round-3 findings against surviving material are fixed: the run CFG node-id collision (a one-token rename, self-verified against the drawn graph) and compare's second state, now bound. Additivity verified against the CR base by all three rounds; both suites stay frozen. All six mechanical checks green.", "frozen": ["eval-run/run/run.feature", "eval-run/compare/compare.feature"]}
