{"kind":"leash","cr":"aced-untradeable-boolean","leash":"auto-all","by":"derived","blast":"small","approach":["CR-7 doctrine KEEP (retro plan, 7-CR queue; source sdd strategy.2d9bbc seq2 item 2, distilled from 263-op6-m4-persona-voice-dimension where the defect recurred to fourth order across five judge rounds): a whole-output untradeable behavior is one boolean @quality scenario, never a @rubric dimension — the gate requires every scenario to pass, so the boolean is strictly stronger than a dimension that can be zeroed and bought back on unrelated points.","Gap identified in explore reconnaissance: the standing Selection bar bans untradeable criteria but its ACED discriminator is same-object and scoped by an 'only' — fail a dimension only when a boolean scenario in the same suite decides that exact property. With no boolean twin present the test cannot fire and the 'only' licenses the defect. The new whole-output test is a second, disjoint entry point beside same-object, read on a SINGLE dimension (does its criterion span the whole output?), never as a comparison between two dimensions — so it does not reintroduce the twin-scan SDD #280 rejected.","Form is already legal and needs no new vocabulary: @quality names the result axis and @rubric is the orthogonal assertion form, so a boolean @quality scenario is Form 1 under sdd:suite-format-governance. CR-7 codifies WHEN to choose it, coining nothing.","Placement: criteria into the aced-builder-spec bar (reference node, no .feature) plus additive scenarios on the scenario-writer producer node and the spec-validator judge node — the corpus pattern every existing Selection criterion follows. The judge node is a scope expansion beyond the brief's two named targets and is flagged for Council at the gate.","In-session conductor: judges cold, synchronous and self-observed with no verdict relayed; gates self-asserted by:agent to this shard, then STOP for Council ratification (by:name) and merge."]}
{"kind":"gate","cr":"aced-untradeable-boolean","gate":"spec","verdict":"approve","by":"agent","self_asserted":true,"leash":"auto-all","judge":"aced-spec-validator (cold, synchronous, self-observed; two independent rounds, round 2 a fresh cold judge re-deriving its own oracle rather than the round-1 agent continued)","why":{"floor":"none — the spec-gate structural classifier reports ADDITIVE on both suites (2 added / 0 modified / 0 removed each). No frozen scenario narrowed or rewritten, so no Clearance floor and no re-open; both suites self-clear and keep their @frozen tag. Root status and approval frontmatter untouched; check-spec-state reports spec states OK.","blast":"small — 4 additive boolean scenarios across two partial-fit role nodes in one project spec (scenario-writer producer, spec-validator judge), plus their README Use Cases / Control Flow / Scenario-map surfaces. No engine, no rubric, no new vocabulary: @quality is established in the SDD glossary and suite-format tag set, orthogonal to @rubric, so the prescribed boolean @quality scenario is already legal Form 1.","novelty":"medium — a second, disjoint entry point into Selection beside the standing same-object test. The standing bar's discriminator is scoped by an 'only' that cannot fire when no boolean twin exists, which is the persona-voice case exactly; the new ground is structural (does the criterion span the whole output?) and needs no trade judgment. Round 2 ran the new rule against the corpus it will police: the three live @quality @rubric scenarios all score per-attribute dimensions of a single artifact, so none is retroactively made non-conforming — the rule does not over-fire on the standing corpus.","confidence":"high — round 2 ALIGNED true, 4/4 PASS, zero open markers remaining. Redundancy tested adversarially against the two nearest standing scenarios and refuted on structure, not rhetoric: the nobody-would-trade ground answers a deterministic rule by inspection, the same-object ground requires a boolean twin the new Given explicitly negates, and the CFG places the span question upstream of the substitutability branch so the grounds cannot overlap. Both negative companions confirmed non-vacuous. Pairwise consistency re-derived across both whole suites. Two judge-iteration corrections, both logged: an unscoped Then that read unconditional against a standing sibling (corrected at the Given so the positive assertion survived; round 2 verified genuine disjointness rather than a narrowed overlap), and a prose surface that stated the pass-branch relationally — the dimension-against-dimension comparison this CR names as its own anti-goal."},"placement":"Judge node included beyond the brief's two named targets, on the round-2 judge's concurrence: the licensing 'only' clause is judge-side text graded backward by the spec-validator, so a producer-only CR would ship a rule with a green judge still passing the defect — the fourth-order recurrence shape. Every standing Selection ground is already doubled producer-side and judge-side. Flagged for Council; droppable on its word.","frozen":[".agents/specs/aced/sdd-roles/scenario-writer/scenario-writer.feature",".agents/specs/aced/sdd-roles/spec-validator/spec-validator.feature"]}
